# Структура репозитория Nexus Protocol

## 🏗️ Двойное версионирование

Проект использует **двойное версионирование** для разделения публичных и приватных компонентов:

### 📖 Публичный репозиторий (GitHub)
Основной репозиторий содержит общедоступные компоненты:
- `api/` - спецификации API (gRPC, REST, WebSocket)
- `deployment/` - конфигурации для развертывания (Docker, Kubernetes, Grafana, Prometheus)
- `protocol/` - документация протокола и спецификации
- `schemas/` - JSON схемы сообщений
- `sdk/` - SDK для различных языков (Go)
- `README.md` - общая документация
- `PROTOCOL.md` - спецификация протокола
- `VERSION` - версия проекта

### 🔒 Локальный сервер (только локально)
Папка `server/` содержит серверную реализацию и **не публикуется** на GitHub:
- Полная реализация Nexus Protocol Server на Go
- Бизнес-логика, обработчики API, сервисы
- Конфигурации базы данных, Redis, AI
- Хранится только локально с Git версионированием

## 🚀 Рабочий процесс

### Для публичных компонентов:
```bash
cd /Users/home/Desktop/@protocol
git add .
git commit -m "Update API documentation"
git push origin main
```

### Для серверного кода:
```bash
cd /Users/home/Desktop/@protocol/server
git add .
git commit -m "Add new authentication endpoint"
# Только локально - без push!
```

## 🔐 Безопасность

- Публичные компоненты доступны всем для изучения и вклада
- Серверный код с бизнес-логикой остается приватным
- Можно безопасно делиться публичными компонентами с сообществом

## 📋 Статус
- ✅ Публичный репозиторий: настроен
- ✅ Локальный сервер: настроен
- ✅ Gitignore: настроен
- ✅ Версионирование: активно</contents>
</xai:function_call">/Users/home/Desktop/@protocol/REPOSITORY_STRUCTURE.md
