# Nexus Protocol Documentation Site

Сайт документации для Nexus Protocol и SDK, построенный на Docusaurus.

## 🚀 Быстрый старт

### Установка зависимостей

```bash
npm install
```

### Запуск в режиме разработки

```bash
npm start
```

Сайт будет доступен по адресу [http://localhost:3000](http://localhost:3000)

### Сборка для production

```bash
npm run build
```

### Предпросмотр production сборки

```bash
npm run serve
```

## 📁 Структура проекта

```
docs-site/
├── docs/                    # Markdown файлы документации
│   ├── protocol/           # Документация протокола
│   └── sdk/                 # Документация SDK
├── src/
│   └── css/
│       └── custom.css      # Кастомные стили
├── static/                  # Статические файлы (изображения, иконки)
├── docusaurus.config.js     # Конфигурация Docusaurus
├── sidebars.js              # Настройка бокового меню
└── package.json
```

## ✏️ Управление контентом

### Добавление новой страницы

1. Создайте новый markdown файл в соответствующей папке (`docs/protocol/` или `docs/sdk/`)
2. Добавьте файл в `sidebars.js` для отображения в меню
3. Сохраните файл - изменения применятся автоматически

### Редактирование существующей страницы

Просто откройте нужный markdown файл в `docs/` и отредактируйте его. Изменения будут видны сразу в режиме разработки.

### Структура markdown файлов

Все markdown файлы должны начинаться с frontmatter:

```markdown
---
id: unique-id
title: Заголовок страницы
sidebar_label: Название в меню
---

Содержимое страницы...
```

## 📚 Организация документации

### Протокол (`docs/protocol/`)

- `intro.md` - Введение в протокол
- `message-format.md` - Формат сообщений
- `metadata.md` - Метаданные
- `error-handling.md` - Обработка ошибок
- `versioning.md` - Версионирование

### SDK (`docs/sdk/`)

- `intro.md` - Введение в SDK
- `installation.md` - Установка
- `quick-start.md` - Быстрый старт
- `usage-guide.md` - Руководство по использованию
- `advanced-guide.md` - Продвинутое использование

## 🎨 Кастомизация

### Изменение темы

Отредактируйте `src/css/custom.css` для изменения цветов и стилей.

### Изменение навигации

Отредактируйте `docusaurus.config.js` для изменения навбара и футера.

### Изменение структуры меню

Отредактируйте `sidebars.js` для изменения структуры бокового меню.

## 📦 Деплой

### GitHub Pages

```bash
npm run deploy
```

### Другие платформы

После сборки (`npm run build`) папка `build/` содержит статические файлы, которые можно задеплоить на любую платформу:
- Netlify
- Vercel
- AWS S3
- И другие...

## 🔗 Полезные ссылки

- [Docusaurus документация](https://docusaurus.io/)
- [Markdown синтаксис](https://docusaurus.io/docs/markdown-features)
- [MDX компоненты](https://docusaurus.io/docs/markdown-features/react)

