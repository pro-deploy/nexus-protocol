# Быстрый старт - Nexus Protocol Documentation Site

## 🚀 За 3 шага

### 1. Установите зависимости

```bash
cd docs-site
npm install
```

### 2. Запустите сайт

```bash
npm start
```

### 3. Откройте в браузере

Сайт будет доступен по адресу: [http://localhost:3000](http://localhost:3000)

## ✏️ Управление контентом

Все содержимое сайта находится в папке `docs/` в виде markdown файлов:

- `docs/protocol/` - документация протокола
- `docs/sdk/` - документация SDK

### Добавить новую страницу

1. Создайте `.md` файл в нужной папке
2. Добавьте frontmatter в начало файла:

```markdown
---
id: my-page
title: Моя страница
sidebar_label: Моя страница
---

Содержимое страницы...
```

3. Добавьте страницу в `sidebars.js` (если нужно)

### Редактировать существующую страницу

Просто откройте нужный `.md` файл в `docs/` и отредактируйте. Изменения применятся автоматически при сохранении.

## 📦 Сборка для production

```bash
npm run build
```

Собранные файлы будут в папке `build/`

## 🌐 Деплой

### Netlify / Vercel

Просто подключите репозиторий - они автоматически обнаружат Docusaurus.

### GitHub Pages

```bash
npm run deploy
```

### Другие платформы

Загрузите содержимое папки `build/` на ваш хостинг.

## 📚 Документация

- [Полный README](./README.md)
- [Docusaurus документация](https://docusaurus.io/)

